const UnauthorizedMessage = 'Authorization required';

const ConflictMessage = 'User with this email already exists';

const DeleteMessage = 'Movie deleted successfully';

const ForbiddenMessage = "It is forbidden to delete other people's movies";

const UserNotFoundMessage = 'User not found';

const MovieNotFoundMessage = 'Movie not found';

const PageNotFoundMessage = 'Page not found';

const InternalServerErrorMessage = 'Internal server error';

const LoginMessage = 'You have succesfully logged in';

const LogoutMessage = 'You have succesfully logged out';

const InvalidMessage = 'Invalid email or password';

const ValidationMessage = 'Validation failed';

const URLMessage = 'URL does not exist';

const EmailMessage = 'Email does not exist';

module.exports = {
  UnauthorizedMessage,
  ConflictMessage,
  DeleteMessage,
  ForbiddenMessage,
  UserNotFoundMessage,
  MovieNotFoundMessage,
  PageNotFoundMessage,
  InternalServerErrorMessage,
  LoginMessage,
  LogoutMessage,
  InvalidMessage,
  ValidationMessage,
  URLMessage,
  EmailMessage,
};
